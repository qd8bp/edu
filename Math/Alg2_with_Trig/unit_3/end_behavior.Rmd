```{r data generation, echo = FALSE, results = "hide"}
## Ugh... this generates errors often...
library(exams)
deg = sample(3:8,1)
nterm = sample(3:deg,1)
coef = sample(2:12,nterm,replace=T)*sample(c(-1,1),nterm,replace=T)
exp = sort(c(sample(0:(deg-1),nterm-1),deg),decreasing = T)
ncs = sum(exp>=2)
s = paste(coef[1],"x^{",exp[1],"}",collapse="")
for(i in 2:nterm){
  op = c("-","+")[(coef[i]>0)+1]
  if(exp[i]>1){
    s = paste(s,op,abs(coef[i]),"x^{",exp[i],"}")
  } else if(exp[i]==1) {
    s = paste(s,op,abs(coef[i]),"x")
  } else {
    s = paste(s,op,abs(coef[i]))
  }
}
if(deg%%2==0 & coef[1]<0){
  sol = "1000"
  odev = "even"
  co1 = "negative"
  explain = "When $x\\to -\\infty$ then $f(x)\\to -\\infty$ and when $x\\to \\infty$ then $f(x)\\to -\\infty$."
} else if(deg%%2==1 & coef[1]<0){
  sol = "0100"
  odev = "odd"
  co1 = "negative"
  explain = "When $x\\to -\\infty$ then $f(x)\\to \\infty$ and when $x\\to \\infty$ then $f(x)\\to -\\infty$."
} else if(deg%%2==1 & coef[1]>0){
  sol = "0010"
  odev = "odd"
  co1 = "positive"
  explain = "When $x\\to -\\infty$ then $f(x)\\to -\\infty$ and when $x\\to \\infty$ then $f(x)\\to \\infty$."
} else if(deg%%2==0 & coef[1]>0){
  sol = "0001"
  odev = "even"
  co1 = "positive"
  explain = "When $x\\to -\\infty$ then $f(x)\\to \\infty$ and when $x\\to \\infty$ then $f(x)\\to \\infty$."
}

```

Question
========

Consider the polynomial function below.

$f(x)=`r s`$

Describe the end behavior.

* When $x\to -\infty$ then $f(x)\to -\infty$ and when $x\to \infty$ then $f(x)\to -\infty$
* When $x\to -\infty$ then $f(x)\to \infty$ and when $x\to \infty$ then $f(x)\to -\infty$
* When $x\to -\infty$ then $f(x)\to -\infty$ and when $x\to \infty$ then $f(x)\to \infty$
* When $x\to -\infty$ then $f(x)\to \infty$ and when $x\to \infty$ then $f(x)\to \infty$

Solution
========

The degree of the polynomial is `r deg`, which is `r odev`. The highest-degree term's coefficient is `r co1`. 

`r explain`

Meta-information
============
extype: mchoice
exsolution: `r sol`
exname: end_behavior
extol: 0
exextra[numwidth,logical]: TRUE
